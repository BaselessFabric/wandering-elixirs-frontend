# This file tells gcloud which files and directories to ignore.
# We want to IGNORE node_modules, but INCLUDE .env.production

node_modules
.git
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Explicitly include .env.production, overriding .gitignore
!.env.production
